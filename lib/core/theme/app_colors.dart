import 'dart:ui';

class AppColors {
  // Background colors (Dark theme)
  static const Color primaryBg = Color(0xFF36393F);      // Main background
  static const Color secondaryBg = Color(0xFF2F3136);    // Sidebar, panels
  static const Color tertiaryBg = Color(0xFF202225);     // Darker elements
  static const Color hoverBg = Color(0xFF3A3C43);        // Hover state

  // Accent colors
  static const Color accentColor = Color(0xFF5865F2);    // Blurple (Discord blue)
  static const Color accentHover = Color(0xFF4752C4);    // Darker blurple

  // Status colors
  static const Color successColor = Color(0xFF3BA55D);   // Green
  static const Color dangerColor = Color(0xFFED4245);    // Red
  static const Color warningColor = Color(0xFFFAA81A);   // Yellow
  static const Color infoColor = Color(0xFF00AFF4);      // Blue

  // Text colors
  static const Color textPrimary = Color(0xFFDCDDDE);    // Main text
  static const Color textSecondary = Color(0xFFB9BBBE);  // Secondary text
  static const Color textMuted = Color(0xFF72767D);      // Muted text
  static const Color textLink = Color(0xFF00AFF4);       // Links

  // Online status
  static const Color onlineStatus = Color(0xFF3BA55D);
  static const Color idleStatus = Color(0xFFFAA81A);
  static const Color dndStatus = Color(0xFFED4245);
  static const Color offlineStatus = Color(0xFF747F8D);

  // Message colors
  static const Color ownMessageBg = Color(0xFF5865F2);
  static const Color otherMessageBg = Color(0xFF2F3136);

  // Divider
  static const Color dividerColor = Color(0xFF3F4147);
}